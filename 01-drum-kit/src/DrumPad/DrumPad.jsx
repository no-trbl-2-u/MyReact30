import React from 'react';

const DrumPad = (props) => {
  return (
    <div>
      {/* <div data-key={dk} className="key" + {addedClass}>
        <kbd>{pressedKey}</kbd>
        <span className="sound">{name}</span>
      </div> */}
    </div>
  );
};

export default DrumPad;